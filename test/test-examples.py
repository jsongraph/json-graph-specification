from jsonschema import FormatChecker, TypeChecker, exceptions, validators
import unittest

# Test examples against JGF
class SchemaTest(unittest.TestCase):


    def test_schema(self):

		# todo: add some reasonable test processing here
        self.assertEqual(
            'fail',
            'fail',
        )
